import React from "react";
import styles from "./Button_Dark.module.css"

const Button_Dark = () => {


    return(
        <a href="#" className={`${styles.btn} ${styles.btnDark}`}>Shop now</a>
    )
}

export default Button_Dark;